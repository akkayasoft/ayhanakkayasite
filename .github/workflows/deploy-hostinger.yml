name: Deploy to Hostinger

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Local-only notice
        run: |
          echo "Remote deployment is disabled. This project is configured for local-only usage."
